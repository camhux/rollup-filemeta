// eslint-disable-next-line @typescript-eslint/naming-convention
export default function DIRNAME(): string {
  throw new Error(
    "UNREACHABLE: encountered an untransformed `rollup-plugin-filemeta/dirname.DIRNAME()` call. Ensure that you've configured `rollup-plugin-filemeta` in your Rollup config correctly.",
  );
}
